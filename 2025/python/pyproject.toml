[project]
name = "aoc-2025"
version = "0.1.0"
description = "AOC 2025"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "polars>=1.35.2",
]

[build-system]
requires = ["uv_build>=0.9.13,<0.10.0"]
build-backend = "uv_build"

[tool.uv]
package = false
